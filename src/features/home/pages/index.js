import { HomeContent } from '../home-content';
import { Container } from './styles';

export const Home = () => {
    return(
        <Container>
            <HomeContent />
        </Container>
    )
}